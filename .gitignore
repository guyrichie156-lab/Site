node_modules
dist
.env
.env.*
.vercel
.DS_Store
*.log

# служебные файлы, не часть исходников сайта
Архив.zip

# локальные файлы Claude Code (разрешения, кэш дизайн-инструмента) —
# специфичны для машины, не часть исходников сайта
.claude/

# посторонний проект (отдельная pnpm-монорепа с мобильным приложением),
# не относится к сайту-портфолио — обнаружен в этой же папке
ecosystem/
