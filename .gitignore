node_modules
dist
.env
.env.*
.vercel
.DS_Store
*.log

# лицензионный аудио-контент — храним локально, в git не кладём
public/*.mp3

# служебные файлы, не часть исходников сайта
Архив.zip

# локальные разрешения Claude Code — специфичны для машины
.claude/settings.local.json
